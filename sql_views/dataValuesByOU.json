{
  "sqlQuery": "select ou.uid, COUNT(*) from datavalue dv inner join organisationunit ou on (ou.organisationunitid = dv.sourceid) where (ou.hierarchyLevel <= 5) group by ou.uid;",
  "description": "Query gathering all the data values per organisation unit id.",
  "type": "QUERY",
  "name": "dataValuesByOu",
  "cacheStrategy": "NO_CACHE"
}
